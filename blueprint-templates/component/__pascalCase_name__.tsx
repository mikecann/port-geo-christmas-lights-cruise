import * as React from "react";
import {Text} from '@mantine/core';

interface Props {
  
}

export const {{pascalCase name}}: React.FC<Props> = ({ }) => {
  return <Text>hello {{pascalCase name}}</Text>;
}